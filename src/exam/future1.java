package src.exam;

public class future1 {
    public static void main(String[] args) {
        int[] dots = {1, 3, 4, 6, 7, 10};
        int[] lines = {2, 2, 2, 2};

        int maxLength = dots[0] + dots[dots.length - 1];

        int right = 0;
        int temp = 0;
        for (int i = 0; i < dots.length; i++) {
            int currentDot = dots[i];

            int length = currentDot;
            while (length <= maxLength) {

            }
        }
    }
}
